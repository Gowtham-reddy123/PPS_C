#include <stdio.h>
int main() {
int length,width,area_of_rectangle;
printf("the length of rectangle in m: ");
scanf("%d",&length);
printf("the width of rectangle in m  : ");
scanf("%d",&width);
area_of_rectangle=length*width;
printf("the area of rectangle is : %d",area_of_rectangle);
return 0;
}
